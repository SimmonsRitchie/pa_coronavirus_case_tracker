import React from "react";

const DataDisplayTitle = ({ title }) => {
  return (
    <div className="data-display-title__container">
      <div className="data-display-title__title">{title}</div>
    </div>
  );
};

export default DataDisplayTitle;
